# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"
gem 'activesupport'
gem 'file-find'
gem 'win32-security' if Gem.win_platform?
gem 'zeitwerk'